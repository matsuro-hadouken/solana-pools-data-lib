/target
/.vscode
/tmp
/examples/research_account_states.rs
# A/B snapshot artifacts — local only, never committed (285MB+ each)
/snapshot-*.json
/snapshot.json
/legacy61.txt
# generator temp files, if a run is interrupted between write and rename
/src/*.tmp
/src/*.lock
